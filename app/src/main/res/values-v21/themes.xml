<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="BlindmanTheme" parent="Theme.AppCompat">
        <item name="android:dialogTheme">@style/ColorPickerDialog</item>
        <!-- This does not work it seems. Programmatically it does. -->
        <item name="android:alertDialogTheme">@style/BlindmanAlertDialog</item>
    </style>

    <style name="BlindmanAlertDialog" parent="">
        <item name="android:windowIsFloating">true</item>
        <item name="android:gravity">center</item>
        <item name="android:layout_gravity">center</item>
        <item name="android:textAlignment">gravity</item>

        <!-- Difficult to get this to work... -->
        <item name="windowFixedWidthMajor">60%</item>
        <item name="windowFixedWidthMinor">60%</item>
        <item name="android:windowMinWidthMajor">60%</item>
        <item name="android:windowMinWidthMinor">60%</item>
    </style>

    <style name="ColorPickerDialog" parent="@style/BlindmanAlertDialog">
        <item name="android:windowNoTitle">false</item>
    </style>
</resources>