<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- The alertDialog item does not seem to work. Programmatically it does. -->
    <style name="BlindmanTheme" parent="android:Theme.DeviceDefault">
        <item name="android:dialogTheme">@style/ColorPickerDialog</item>
        <item name="android:alertDialogTheme">@style/BlindmanAlertDialog</item>
    </style>

    <style name="BlindmanAlertDialog" parent="android:Theme.DeviceDefault.Dialog.NoActionBar">
        <item name="android:gravity">center</item>
        <item name="android:layout_gravity">center</item>
        <item name="android:textAlignment">gravity</item>
    </style>

    <style name="ColorPickerDialog" parent="@style/BlindmanAlertDialog">
        <item name="android:windowNoTitle">false</item>
        <item name="android:windowIsFloating">true</item>
    </style>
</resources>