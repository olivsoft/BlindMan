<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- The item styles do not seem to work. Programmatically, they do. -->
    <style name="BlindmanTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:dialogTheme">@style/ColorPickerDialog</item>
        <item name="android:alertDialogTheme">@style/BlindmanAlertDialog</item>
    </style>

    <style name="BlindmanAlertDialog" parent="android:Theme.DeviceDefault.Dialog.NoActionBar.MinWidth">
        <item name="android:gravity">center</item>
        <item name="android:layout_gravity">center</item>
        <item name="android:textAlignment">gravity</item>
        <item name="android:windowMinWidthMajor">10%</item>
        <item name="android:windowMinWidthMinor">10%</item>
    </style>

    <style name="ColorPickerDialog" parent="android:Theme.DeviceDefault.Dialog.MinWidth">
        <item name="android:gravity">center</item>
        <item name="android:layout_gravity">center</item>
        <item name="android:textAlignment">gravity</item>
        <item name="android:windowMinWidthMajor">10%</item>
        <item name="android:windowMinWidthMinor">10%</item>
    </style>
</resources>